<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.helloworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".OverzichtTerras2"
            android:screenOrientation="landscape"
            android:label="@string/titel_terras2"/>
        <activity android:name=".OverzichtTerras1"
            android:screenOrientation="landscape"
            android:label="@string/titel_terras1"/>
        <activity
            android:name=".OverzichtCafe"
            android:screenOrientation="landscape"
            android:label="@string/titel_cafe" />
        <activity
            android:name=".OverzichtRestaurant"
            android:screenOrientation="landscape"
            android:label="@string/titel_restaurant" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>